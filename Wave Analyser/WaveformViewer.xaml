<UserControl x:Class="Wave_Analyser.WaveformViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Wave_Analyser"
             mc:Ignorable="d" 
             d:DesignWidth="1000" Height="150"
             Focusable="True"
             Background="Transparent">
    <Grid>
        <Canvas  x:Name="background" Background="#FCFCFC" ClipToBounds="True" Margin="0,0,0,15"/>
        <ScrollViewer x:Name="viewer" VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Visible">
            <Grid x:Name="theGrid"
        MouseDown="Grid_MouseDown"
        MouseUp="Grid_MouseUp"
        MouseMove="Grid_MouseMove"
        Background="Transparent" Margin="0,0,0,0">
                <Canvas x:Name="timeDomainGraph" HorizontalAlignment="Left" Background="Transparent" ClipToBounds="True"/>
                <Canvas>
                    <Rectangle
                x:Name="selectionBox"
                Visibility="Collapsed"
                Stroke="Black"
                StrokeThickness="1"
                />
                </Canvas>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
