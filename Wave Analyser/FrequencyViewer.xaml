<UserControl x:Class="Wave_Analyser.FrequencyViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Wave_Analyser"
             mc:Ignorable="d" 
             d:DesignWidth="1000" d:DesignHeight="300"
             Focusable="True"
             Background="Transparent">
    <Grid>
        <Canvas  x:Name="background" Background="#FCFCFC" ClipToBounds="True" Margin="0,0,0,15"/>
        <ScrollViewer x:Name="fViewer" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <Grid Margin="0,0,0,0">
                <Canvas x:Name="frequencyDomainGraph" HorizontalAlignment="Left" Background="Transparent" ClipToBounds="True"/>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
